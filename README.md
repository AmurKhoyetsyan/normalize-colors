# normalize-colors

## Instructions on how to install a package

    To install it, you need to type npm install normalize-colors or npm i normalize-colors in the terminal.
    In the program, you need to import it like this:
        import normalizeColor from "normalize-colors";
